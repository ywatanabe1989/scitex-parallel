Installation
============

Requirements
------------

- Python >= 3.10

Basic Installation
------------------

.. code-block:: bash

   pip install scitex-parallel

Development
-----------

.. code-block:: bash

   git clone https://github.com/ywatanabe1989/scitex-parallel.git
   cd scitex-parallel
   pip install -e ".[dev]"
